<h1>Posts</h1>
<% @posts.each do |post| %>
    <div>
        <%= post.content %>
        <% if logged_in? %>
            <%= post.user.name %>
        <% end %>
    </div>
<% end %>
<%= link_to "new", new_post_path %>
